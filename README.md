
  # Build base app shell

  This is a code bundle for Build base app shell. The original project is available at https://www.figma.com/design/JdKgknLmsr26cGXcEFGbGb/Build-base-app-shell.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  